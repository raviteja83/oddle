{
  "name": "oddle",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "7.7.1",
    "npm": "4.1.2"
  },
  "devDependencies": {
    "react-scripts": "0.9.4"
  },
  "dependencies": {
    "express": "^4.15.2",
    "halogen": "^0.2.0",
    "jquery": "^3.1.1",
    "react": "^15.4.2",
    "react-dom": "^15.4.2",
    "react-infinite-scroller": "^1.0.7",
    "react-router": "^3.0.2"
  },
  "scripts": {
    // "start":"rm -rf node_modules && yarn && yarn run build && node server.js",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
